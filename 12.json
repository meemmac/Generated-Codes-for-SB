{
"title": "Prim's Algorithm",
"subtitle": "Find the Minimum Spanning Tree (MST) of a graph starting from any node",
"time_complexity": "O(E log V)",
"space_complexity": "O(V)",
"category": "Graph Algorithm",
"code": [
    "import heapq",
    "",
    "def prim(graph, start=0):",
    "    visited = set([start])",
    "    edges = [(weight, start, to) for to, weight in graph[start]]",
    "    heapq.heapify(edges)",
    "    mst = []",
    "",
    "    while edges:",
    "        weight, frm, to = heapq.heappop(edges)",
    "        if to not in visited:",
    "            visited.add(to)",
    "            mst.append((frm, to, weight))",
    "            for next_to, w in graph[to]:",
    "                if next_to not in visited:",
    "                    heapq.heappush(edges, (w, to, next_to))",
    "",
    "    return mst"
],
"additional_information": "Prim's algorithm grows the MST one vertex at a time by always choosing the minimum weight edge that connects to the growing tree."
}
