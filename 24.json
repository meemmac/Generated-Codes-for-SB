{
"title": "Sliding Window Maximum",
"subtitle": "Find the maximum value in every subarray of size k",
"time_complexity": "O(N)",
"space_complexity": "O(k)",
"category": "Array / Sliding Window",
"code": [
    "from collections import deque",
    "",
    "def sliding_window_max(nums, k):",
    "    result = []",
    "    dq = deque()",
    "",
    "    for i, num in enumerate(nums):",
    "        while dq and dq[0] <= i - k:",
    "            dq.popleft()",
    "        while dq and nums[dq[-1]] < num:",
    "            dq.pop()",
    "        dq.append(i)",
    "        if i >= k - 1:",
    "            result.append(nums[dq[0]])",
    "",
    "    return result"
],
"additional_information": "This technique uses a deque to maintain a decreasing sequence of elements’ indices to efficiently compute maximums in each window."
}
