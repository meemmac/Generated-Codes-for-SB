{
"title": "Longest Common Subsequence (LCS)",
"subtitle": "Find the longest sequence present in both strings in order",
"time_complexity": "O(M × N)",
"space_complexity": "O(M × N)",
"category": "Dynamic Programming",
"code": [
    "def lcs(X, Y):",
    "    m = len(X)",
    "    n = len(Y)",
    "    dp = [[0] * (n + 1) for _ in range(m + 1)]",
    "",
    "    for i in range(1, m + 1):",
    "        for j in range(1, n + 1):",
    "            if X[i - 1] == Y[j - 1]:",
    "                dp[i][j] = dp[i - 1][j - 1] + 1",
    "            else:",
    "                dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])",
    "",
    "    return dp[m][n]"
],
"additional_information": "The LCS problem is fundamental in diff tools, bioinformatics, and version control to measure similarity between sequences."
}
