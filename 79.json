{
"title": "Flood Fill Algorithm",
"subtitle": "Fill a connected region in a grid with a target color/value",
"time_complexity": "O(M*N)",
"space_complexity": "O(M*N) recursion stack in worst case",
"category": "Graphs / DFS",
"code": [
    "def flood_fill(grid, x, y, new_color):",
    "    m, n = len(grid), len(grid[0])",
    "    original_color = grid[x][y]",
    "    if original_color == new_color:",
    "        return grid",
    "    def dfs(i, j):",
    "        if i < 0 or i >= m or j < 0 or j >= n or grid[i][j] != original_color:",
    "            return",
    "        grid[i][j] = new_color",
    "        dfs(i+1, j)",
    "        dfs(i-1, j)",
    "        dfs(i, j+1)",
    "        dfs(i, j-1)",
    "    dfs(x, y)",
    "    return grid"
],
"additional_information": "Flood fill can be implemented with DFS or BFS. It’s commonly used in image processing to fill connected regions."
}
