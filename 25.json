{
"title": "Two Pointers Technique",
"subtitle": "Solve array problems by using two indices moving through the array",
"time_complexity": "O(N)",
"space_complexity": "O(1)",
"category": "Array / Two Pointers",
"code": [
    "def two_sum_sorted(nums, target):",
    "    left, right = 0, len(nums) - 1",
    "    while left < right:",
    "        current = nums[left] + nums[right]",
    "        if current == target:",
    "            return (left, right)",
    "        elif current < target:",
    "            left += 1",
    "        else:",
    "            right -= 1",
    "    return None"
],
"additional_information": "Two pointers technique is commonly used in sorted arrays or linked lists to find pairs, triplets, or to solve subarray problems efficiently."
}
