{
"title": "Manacher’s Algorithm",
"subtitle": "Find the longest palindromic substring in linear time",
"time_complexity": "O(N)",
"space_complexity": "O(N)",
"category": "String / Palindrome",
"code": [
    "def manacher(s):",
    "    t = '#' + '#'.join(s) + '#'",
    "    n = len(t)",
    "    p = [0] * n",
    "    center = right = 0",
    "",
    "    for i in range(n):",
    "        mirror = 2 * center - i",
    "        if i < right:",
    "            p[i] = min(right - i, p[mirror])",
    "        a, b = i + p[i] + 1, i - p[i] - 1",
    "        while a < n and b >= 0 and t[a] == t[b]:",
    "            p[i] += 1",
    "            a += 1",
    "            b -= 1",
    "        if i + p[i] > right:",
    "            center, right = i, i + p[i]",
    "",
    "    max_len = max(p)",
    "    center_index = p.index(max_len)",
    "    start = (center_index - max_len) // 2",
    "    return s[start:start + max_len]"
],
"additional_information": "Manacher’s algorithm transforms the string to handle even-length palindromes uniformly and computes the longest palindromic substring in linear time."
}
